// `search-sdk` is an alias package: it re-exports the entire public surface of
// `@search-sdk/core` so consumers can install/import `search-sdk` directly and
// get the same WebSearch class, types, errors, and built-in AI SDK adapter.
export * from "@search-sdk/core";
